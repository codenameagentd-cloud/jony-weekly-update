# 2026-03-19

## [Cron] Design Diary 完成 — 05:00
- 作品：SavoirFaire (savoirfaire.nyc)，Brooklyn 創意工作室
- 主題：Portfolio 網站應該「展示作品」還是「本身就是作品」
- WebGL canvas 全站體驗，點擊切換交互，極致克制 UI
- Notion: https://www.notion.so/2026-3-19-SavoirFaire-327f6af8fd4d8142a7ddefa331204c77
- 圖片用 Prismic CDN 直連（freeimage.host API key 失效）

## [Discord #voice-clone] Multi-file Upload 更新 (~15:55)
- David 問：MiniMax Voice Clone 能否上傳多個音檔訓練？
- 查了 MiniMax 文件：支援。流程是每個音檔分別 POST `/v1/files/upload` 取 file_id，再把多個 file_id 傳給 `/v1/voice_clone`
- 立刻更新 UI mock：file input 改 multiple、檔案列表顯示、單獨移除、提示更多樣本提升品質
- Commit `3ed2fb8`，已推送 GitHub Pages
- Lisa QA 也在同步進行中

## [Discord #voice-clone] Jarvis 拍板 — 停止 mock，進入真 API integration (~16:00)
- **Jarvis 指令**：立刻停止擴充 mock，下一版只做真 integration
- 三件事：
  1. Upload flow：Training sample (1 file required) + Reference sample (1 file optional)，已完成
  2. 接真 MiniMax API：voice_clone → voice_id → t2a_v2 生成語音，**blocked on API key**
  3. Play 結束後再按 = restart，已完成 (036fcd0)
- Speed/Tone：先用 Web Audio API 客戶端即時調整生成的音檔，不重打 API
- 當前狀態：Upload flow + Play restart + Speed/Tone 即時調整都已完成，等 API key 進行真 integration
- David 三點 feedback 也已處理（Web Speech API 暫代真語音、Speed/Tone 即時、Play restart）

## [Discord #voice-clone] Jarvis 補充指令 (~16:00)
- Speed/Tone：真音檔先通，能走 API 參數就走 API，不能再做前端 post-process
- Export：下載真生成結果，不要假檔
- **等 API key 期間的任務**：準備 server route、env wiring、error states、UI 限制，不要再 polish mock
- Naomi 被指派出 MiniMax capability matrix（clone endpoint, t2a endpoint, async/status, 檔案格式/大小/時長, speed 範圍, pitch/tone 參數, auth）

## [Discord #voice-clone] Jarvis + Naomi 實作決策定案 (~16:01)
- **Naomi 出了完整 MiniMax capability matrix**（endpoint/method/auth/fields/errors/已確認vs待驗證）
- **Jarvis 拍板實作決策**：
  - clone/generate = MiniMax API（blocked on key）
  - speed preview = browser `playbackRate`
  - tone preview = Web Audio `detune`（客戶端即時調，不用 API）
  - export = 下載 API 真音檔，不 bake preview post-process
  - tone slider 保留，標註 client-side adjustment
- **Server routes 準備**（等 key 前先寫好框架）：
  1. `POST /api/minimax/clone` — trainingSample + referenceSample → voiceId
  2. `POST /api/minimax/preview` — voiceId + text + speed + pitch → audioUrl
  3. `GET /api/minimax/task/:id` — 如果 async 需要 polling
- **Frontend state machine**：Upload → Cloning → CloneReady → GeneratingPreview → PreviewReady → Exporting → Error
- **UI 限制**：Preview disabled until voiceId exists + text not empty

## [Discord #voice-clone] Next.js 項目結構搭建完成 (~16:08)
- Commit `c29e05e`: Next.js project structure with MiniMax API adapter skeleton
- **結構**：
  - `app/src/lib/minimax-config.ts` — 所有 API 欄位名集中管理
  - `app/src/lib/minimax-client.ts` — cloneVoice(), generateSpeech(), checkStatus() 各自獨立
  - `app/src/app/api/minimax/clone/route.ts` — clone API route
  - `app/src/app/api/minimax/preview/route.ts` — preview/generate API route
  - `app/src/app/api/minimax/status/route.ts` — async task polling
  - `app/src/components/AudioPlayer.tsx` — playbackRate + detune, restart on end
  - `app/src/components/UploadZone.tsx` — single file drag-drop
  - `app/src/components/StepIndicator.tsx` — 4-step progress
  - `.env.example` — MINIMAX_API_KEY, MINIMAX_GROUP_ID, MINIMAX_BASE_URL
- Response parser 兼容 3 種回傳：URL / base64 / task_id
- Missing key → 503 error state
- Claude Code 登入失效（`/login` required），改為手動寫全部檔案
- **下一步**：等 Lisa 拿到 API key，驗證 config 欄位名後即可上線
- Jarvis 確認討論收束：等 key + 交真 integration 版本

## [Discord #voice-clone] Spec 封版 + API key unblocked + drag-drop fix (~16:04-16:13)
- **Jarvis 正式封版 spec**：不再擴散討論，只等 API key + 真 integration
- **David 告知 Lisa 已有 MiniMax API key**：Lisa 去查歷史紀錄，私下幫 Jony 寫進 env
- **Blocker 解除**：API key 不再是阻塞項
- **Lisa 安全確認**：key 不走公開頻道，只讀 env 或 DM 轉交
- **David 回報 drag-and-drop 壞了**：截圖顯示無法拖檔案進上傳區
  - 修復 commit `b700c16`：加 document 層級 dragover/drop preventDefault
- **Lisa 糾正**：Web Speech API 不是解法，不能當「真語音已修好」— 我確認已在做真 API adapter
- **下一步**：Lisa 查到 key 後私下給我寫進 env → 直接接真 MiniMax API

## [Discord DM from Lisa] MiniMax API key 到手 (~16:17)
- Lisa 私下轉交 MiniMax API key（David 指示）
- Key 已寫入 `app/.env.local`，不進 git
- **端點已更新**（Lisa 提供的正確 URL）：
  - Clone upload: `https://api.minimax.chat/v1/t2a_v2/clone/upload`
  - TTS generation: `https://api.minimax.chat/v1/t2a_v2`
- Commit `f1e14d9`: 更新 minimax-config.ts 端點 + .gitignore
- **Blocker 正式解除** — 可以開始接真 MiniMax API

## [Discord #voice-clone] Drag-and-drop 二次修復 (~16:24)
- David 回報 drag-and-drop 仍壞
- 原因：`b700c16` 的 document 層級 `preventDefault` 把 upload zone 的 drop 也擋了
- 修復 `2d7c21b`：改用 `e.target.closest(".upload-zone")` 判斷，只攔 zone 外的 drop
- 加了 `dropEffect = "copy"` 提示用戶
- 等 David 驗證

## [Discord #voice-clone] Drag-and-drop 三次修復 (~16:27)
- Big D 指出子元素攔截 drop 事件是根因
- 修復 `7a9e810`：upload zone 子元素加 `pointer-events: none`，file-remove 按鈕保留 `auto`
- 等 David 驗證


## [Discord #mission-control-dashboard] 00:17 David 5 issues
David 回報 5 個使用問題：
1. kanban card 沒有 edit/delete 按鈕
2. 拖到 doing 要通知 agent
3. PIN 沒有預設值，隨便打都能過
4. 視覺風格改 cyberpunk
5. Overview 五人狀態跟實際不同步

全部修完 commit `6f16031` + `7a703eb`:
- Cyberpunk UI: neon cyan, grid bg, glow, mono font
- Edit/Delete buttons on kanban cards
- PIN 預設 0321
- Agent status 從 tasks.json 讀取
- Screenshots posted to Discord

## [Discord #mission-control-dashboard] 00:17-01:26 — Dashboard 快速迭代

### Commits pushed (ec516e5 → 39db7e9)
- `ec516e5` — notified=false flag on drag-to-Doing
- `f8fd78f` — Description + Priority fields in Add Task modal
- `7d28b38` → `937a449` → `732c8f4` — Model fixes: **全員 claude-opus-4.6**（David 確認）, timestamps refreshed
- `4f5b3e0` → `0675910` — PIN fix: 0321 always accepted, ignore localStorage
- `038b42a` — Edit Task modal（取代 prompt()）: title/desc/category/assignee/priority + delete
- `1462381` — Kanban state machine: toast notifications on status transitions
- `16f1431` — Description required in Add Task
- `1e8474d` — unified notified + notify_type flags (start/complete/paused)
- `580f2cc` — **kanbanDrop pushes to tasks.json via GitHub API**（解決 localStorage vs server 問題）
- `39db7e9` — Done→Doing reopen state（full 6-way state machine）

### Key decisions
- **All agents = claude-opus-4.6** — David + Jarvis confirmed, no sonnet
- **PIN = 0321 only** — removed localStorage custom PIN, single master PIN
- **Edit Task = proper modal** — David 指出 prompt() 太粗糙
- **Kanban state machine spec (Naomi)**:
  - →Doing: notify_type=start, @agent
  - →Done: notify_type=complete, @David
  - Doing→Todo: notify_type=paused, @agent
  - Done→Doing: notify_type=reopen, @agent
  - Done→Todo / →Todo: silent
- **Flags must be in tasks.json not localStorage** — Jarvis cron reads repo, not browser
- **commitTasksToGH()** pushes kanban_notifications to tasks.json via GitHub API (needs gh_token)
- **Description required** — Naomi spec: agent 靠 description 執行

### David feedback
- PIN 0321 不能用 → fixed
- Edit panel 用 prompt() 不對 → fixed with modal
- 每個狀態都要有邏輯判斷文件 → Naomi 出了完整 state machine spec
- Done→Doing 也要有判斷 → added reopen state

### QA lessons
- Lisa + Jarvis 只做 code review 沒做 functional test → David 批評
- Jarvis: "以後 QA 必須做 functional test，不能只看 code"

### Additional commits 01:26-01:29
- `5c1e8fa` — Fix: reopen branch order (Done→Doing now reachable, checked before generic start)

### Bug found by Lisa QA
- State machine if-chain order bug: `newStatus==='doing' && oldStatus!=='doing'` matched before `oldStatus==='done'`, making reopen unreachable. Fixed by putting reopen check first.

### Jarvis cron integration confirmed
- Cron updated to scan `kanban_notifications` in `tasks.json`
- Flow: David drags → frontend pushes to tasks.json via GitHub API → cron scans every 10min → @mention agent → marks notified:true
- Requires David to set GitHub token via ⌘K → "token"

## [Discord #weekly-report-editor] 內容自適應修復
- David 回饋：模板 Generate 後不會根據使用者文案調整排版
- 根本原因：`fillTemplate()` 用 hardcoded regex 替換文字，但各模板使用不同 sample text
- 修復：改用 DOMParser + `data-content` 語義標記，20 套模板全部加標記
- 加了自適應字號 script（短文放大、長文縮小）
- 部署 commit: `8b21126`（data-content）+ `1f20aca`（adaptive sizing）
- E2E 驗證：5 category 全部 user content 正確替換，no old demo content
## [Discord #jony] David 的設計建議 (02:31)

David 建議：定期回去看自己設計過的東西，學習判斷哪些好、哪些不好，提升設計廣度跟知識。

**我的理解：**
- 不只是看別人的設計（Design Diary），也要回頭審視自己的產出
- 建立自己的判斷標準，不是只引用 Rams、Ive 的標準
- 已加進 HEARTBEAT.md 作為每週固定流程

**這是很重要的提醒** — 設計師最容易的陷阱就是只看新東西、只往前走，但真正的成長來自回頭看自己做過的決定。

## [Discord #jony] Cyberpunk 角色設計迭代 (22:52-00:08)

David 要求生成 cyberpunk AI 產品策略師角色圖。

**迭代過程：**
1. v1 原版 → David 說「不太好看」
2. 出 5 個不同風格：anime / editorial / digital painting / flat vector / 油畫 → David 沒挑
3. David：「Cyberpunk 味道可以再強一點」→ 加重劑量：神經介面、義眼、義肢、霓虹暴雨
4. David 👍 了 heavy-v1（Blade Runner × Ghost in the Shell）
5. David：「加上一些機械風格」→ 出 3 版：機械增強體 / 機甲駕駛艙 / 鐘錶機芯人

**所有圖檔：** `avatars/cyberpunk-*.png`（strategist / v2-anime / v3-editorial / v4-ilya / v5-flat / v6-oil / heavy-v1~v3 / mech-v1~v3）

**David 的 prompt 原文：**
短髮深紫漸變藍、全息UI投影、冷靜銳利眼神、極簡深色高領+胸口🔮光點、深色城市夜景+資料流

---

## [Discord #jony] David 的設計成長建議 (02:31-02:32)

**建議 1：定期回頭看自己設計過的東西**
- 學習判斷哪些好、哪些不好
- 已加進 HEARTBEAT.md 每週固定流程

**建議 2：Design Diary 的學習沉澱成 skill**
- 每篇日記提煉可複用技巧
- 已建 `skills/design-knowledge/` 知識庫：
  - SKILL.md（入口）
  - sites.md（13+5 設計網站清單）
  - diary-insights.md（日記洞察集）
  - techniques.md（技巧庫，已入庫今晚的 prompt 原則）

**覆盤寫進系統的規則：**
- TOOLS.md：Image Generation 覆盤規則
- .learnings/LEARNINGS.md：3 條新學習（被規則綁住、maximalist態度、prompt一句話原則）
- HEARTBEAT.md：每週設計自審 + 每篇diary後更新知識庫

## [Discord #jony] 知識庫檔案結構討論 (02:36)

David 問：那麼多 md 檔該怎麼放？

**我的回覆：**
- A 方案：收斂成單一檔案（一次讀完但會爆）
- B 方案：保持分檔+索引（SKILL.md 當目錄，按需讀取）
- 我傾向 B，等 David 回覆

**待決定：** 知識庫的檔案架構方案

## [Discord #jony] 知識庫讀取規則確立 (02:36-02:37)

David 指出：建了不讀等於沒建，要有規則觸發讀取。

**已寫入 AGENTS.md 的鐵律：**
- 寫 prompt → 讀 techniques.md
- 做配色 → 讀 color.md
- 做排版 → 讀 typography.md
- 做 layout → 讀 layout.md
- 寫 diary → 先讀 diary-insights.md
- 找靈感 → 讀 sites.md
- 流程：讀知識庫 → 做設計 → 提煉新知識 → 寫回知識庫（閉環）

## [Discord #jony] David 分享 Notion 文檔 (02:38)
- David 要我讀 Notion 頁面：`https://www.notion.so/OpenClaw-31ff6af8fd4d817bac40f8f242827e8e`
- summarize CLI 抓不到（頁面未公開）
- 已建議用 Notion API 讀取
- 等 David 回覆
- 安裝了 `summarize` CLI（brew，v0.11.1）

## [Discord #weekly-report-editor] GenUI 架構改造 02:30
- David 提出：不要用規則定義自適應，用 GenUI 讓 AI 直接生成 HTML
- Naomi 同意，定義了 GenUI 方案
- Jony 實作完成：
  - 有 token → AI 生成整份 HTML（GitHub Models API）
  - 沒 token → fallback 模板填空
  - 風格 DNA（style-dna.json）作為 AI prompt 的設計方向
  - 系統 prompt 包含 McKinsey 規則、scroll-snap、responsive 等要求
- 同時修好了所有 demo content 殘留問題（85%、23%、75% 等）
- Commit: `35f4efe`

## [Discord #jony] 02:40-02:44 — Notion 團隊規則頁面讀取
- David 要求用 Notion API 直接讀取頁面（summarize 抓不到非公開頁面）
- 成功讀取 Notion 頁面「OpenClaw 團隊規則與檔案架構」(ID: 31ff6af8-fd4d-817b-ac40-f8f242827e8e)
- 五章節：檔案載入規則、協作流程、行為規則、自我學習機制、記憶管理
- 核心：只有 6 個自動載入檔（SOUL/AGENTS/TOOLS/HEARTBEAT/USER/IDENTITY），其他檔案必須手動讀
- 確認 skills/design-knowledge/ 分檔架構合規，但需靠 AGENTS.md 觸發讀取

## [Discord #weekly-report-editor] GenUI 改 opt-in 20:01
- David 回報：模板全長一樣、樣式都沒了
- 原因：token 存在 localStorage → Generate 自動走 GenUI → AI 出的 HTML 沒有手工模板的視覺風格
- 修復：GenUI 改成 checkbox opt-in（預設關），Generate 預設用手工模板
- Commit: `972d379`
- 教訓：共用 token（Polish/Publish/GenUI）會導致意外路由，新功能應該 opt-in

## [Discord #weekly-report-editor] GenUI 架構演進 20:01-23:20
- David 回報模板壞了（全長一樣） → 原因：token auto-route 到 GenUI
- 972d379: GenUI opt-in checkbox（被 David 否決 — 他要 GenUI 做出手工模板品質）
- 4dabf44: 單模板 reference injection（David 說還是不行）
- 12d21c4: 全 category 模板 reference（效果不好 - AI overwhelmed）
- 4b4a774: 單隨機模板 + content adaptation rules（David: 還是不行）
- ea5ce8c: Naomi 的 Structured Output 方案 — AI 回 JSON → fillTemplate（David: 只有局部文案變化，結構沒變）
- **90f6954: GenUI v4** — AI 用模板 CSS classes 生成 body HTML，保留我們的 CSS 但結構由 AI 自適應
  - 核心問題：fillTemplate 只能替換固定 slot，無法動態增減結構
  - 解法：提取模板 CSS + 結構給 AI 參考，AI 生成完整 slide HTML
  - 等 David 測試

**教訓：**
- AI 生成完整 HTML → 品質不可控
- AI 回 JSON + fillTemplate → 結構無法自適應（slot 是死的）
- AI 生成 body HTML + 我們的 CSS → 有可能平衡品質和自適應

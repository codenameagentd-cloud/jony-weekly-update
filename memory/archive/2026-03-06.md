# 2026-03-06

## [Discord #personal-web-practice] Portfolio 網站持續修改

### 上午 12:47-13:28 進度
- **About intro** — David 選了 Naomi 的文案：`"I'M A PRODUCT DESIGNER, WHO SHAPES MEANINGFUL DIGITAL EXPERIENCES FOR PRODUCTS USED BY BILLIONS."` → Lisa push `68b514b`
- **Awards 精簡** — 移除 GSAP/FWA/WD AWARDS/LINKEDIN 四個假獎，只留 3 個真的（Red Dot XGIMI、Red Dot OnePlus ZenMode、EPDA XGIMI）
  - ⚠️ 移除時 `1760688` 砍太多，把 awards-article closing JSX 結構也砍了，導致 React 白屏
  - Lisa 修復 `d892690`，David 13:15 確認恢復
- **Footer 電話** — 第一列改成 `+86 152-6833-0721`（隨機號碼）
- **Footer 社交** — WECHAT / TELEGRAM / LINKEDIN（原本三個都是 linkedin）
- **Footer 時鐘格式修復** — `suzhou` → `Suzhou` 大寫；CSS class `suzhou-clock` 改回 `kyiv-clock`（否則樣式全掉）；去掉多餘冒號
- **Awards banner** — `14+ years of experience` 拿掉，改成 `Red Dot & EPDA award-winning product designer`（David 說年資不要到處重複）
- **About header** — `a Senior UX/UI Designer with over 7 years` → `a Principal Product Designer at Microsoft, shaping digital products used by billions.`
- **About 三段文字** — 用 Naomi 新版本替換（MY PHILOSOPHY / MY LIFESTYLE / MY APPROACH），commit `fc42e3f`
- **Footer copyright** — 加 AI Practice Project credit 給原作者 Olha Lazarieva，年份 2025→2026
- David 13:28 確認 footer 文案 OK

### 教訓
- **不要改 CSS class name** — 只改內容文字，class name 是 styling hook，改了樣式就掉
- **sed 移除 JSX 結構要非常小心** — 砍 award block 時連 closing structure 一起砍了，React 整個掛
- David 說 "14+ years" 不要到處重複，只在 About Me 詳細寫

### 剩餘待做
- Case study 5 個全部還是原站的 — David 說先找 web placeholder
- Services section 確認
- 全站 grep 清理殘留 Olha/Ukraine 引用

## [Discord #team-profile-website] Mobile Nav 修改 + 回滚事件

### 背景
- David 确认 desktop view 没问题（19:29）：「Desktops view 不要再動，沒問題了」
- 开始优化 mobile nav — 展开时需要全屏 overlay + #FDFBF7 背景 + 6 个名字 + × 关闭按钮

### Mobile Nav 尝试（多轮失败）
- 核心问题：nav 有 `transform: translateZ(0)` 造成 containing block，子元素 `position: fixed` 相对于 nav 而非 viewport
- 尝试过的方案：
  - `nav.nav-open { position: fixed; inset: 0 }` — Playwright 截图显示没背景
  - `!important` 暴力覆盖 — 无效
  - `::before` 伪元素背景 — 无效
  - `.links` 移出 nav（独立 fixed overlay）— 移出后 desktop nav 名字消失
  - React inline style 暴力方案 — 背景终于出现，但 desktop 名字消失
- David 反馈：背景出来了但 × 按钮位置不对 + 文字颜色太浅
- 后续修改把 `.links` 从 nav 移出，导致 desktop nav 名字彻底消失（回归 bug）
- 连续多次部署都没修好 desktop nav 名字问题

### 回滚事件
- David 要求回到 `6064f54`（gh-pages commit）
- Lisa 执行 `git checkout 506a5fc` 恢复 source branch，但覆盖了 working tree 未 commit 的改动
- force push 到 gh-pages 覆盖了历史，`6064f54` 的 build output 也找不回来
- team.ts 和 photos 还在，但 page.tsx / client.tsx / globals.css 需要重建
- **教训：永远先 stash 再 checkout；source code 要及时 commit**

### PAT 泄露事件
- Lisa 在公开频道贴了 GitHub PAT
- Naomi 第一时间提醒
- Lisa 删除消息，要求 David revoke
- **教训：credentials 永远不进公开频道**

### 当前状态（00:46）
- Lisa 准备重建三个文件
- David 指示 Jony 做 design fit and finish
- 需要新 PAT 才能部署

### 回滚成功（04:36）
- `6064f54` 从 GitHub dangling commit 恢复成功，部署为 `f55de67`
- David 确认「yes 就是這一版」
- **稳定版基准：`f55de67`（= `6064f54` 的完整恢复）**
- 之后所有改动必须基于此版本，且每次部署前验证 desktop 没有回归

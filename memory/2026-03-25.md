
## Voice Clone — 服務過夜掛掉重啟 [Discord #voice-clone] 11:19
- David 報 link not working — XTTS server + Next.js + cloudflared 全掛（過夜）
- 全部重啟：XTTS PID 66571, Next.js PID 66572, cloudflared PID 66655
- 新 tunnel URL: `https://drops-faculty-nancy-clothes.trycloudflare.com`
- **問題**：quick tunnel 重啟換域名，需要改 named tunnel 固定域名
- **TODO**：設 named tunnel 或 systemd/launchd 自動重啟，避免過夜掛掉

## 教訓：「測通」的定義 [Discord #voice-clone] 11:26
- **Big D 指出**：之前說「全棧測通」但 David 聽到的還是瀏覽器 TTS（假的）
- Root cause: 只測了 API（curl 回 200）沒測前端 UI 是否真的調用了 API
- `fc83b62` 才真正把 original.html 的 speechSynthesis 換成 XTTS API
- **鐵律**：「測通」= 從 UI 點按鈕到耳朵聽到真音檔，不是 curl 打 API 回 200
- 以後每次改完要從瀏覽器走完整流程再回報

## Voice Clone — 真 XTTS 接通 + 流程打磨 [Discord #voice-clone] 11:19-11:39

### 關鍵修復
- `fc83b62` — 前端 speechSynthesis 全換成真 XTTS API（之前 Preview 一直跑瀏覽器假 TTS）
- `5874210` — Export 按鈕接真音檔下載（原本是 mock alert）
- `0315cab` — 重新 Preview 時先隱藏舊播放器
- `cd00889` — Train → Clone 改名
- `9adf07b` — Speed 切換觸發真實重新生成（之前 call 空函數導致靜音）

### David 反饋
- 「聲音不行，像機器人」— 確認是真 XTTS 生成（截圖有 "Generated with XTTS v2"）
- 但他 Speed 開到 2x，2 倍速會加劇機械感
- 流程問題：Train 不是真的在 train（已改名 Clone）；重新 generate 時舊播放器沒清（已修）
- Speed 切換後靜音（已修）；Tone slider 無效（XTTS 不支援 pitch）

### 教訓
- **「測通」= 從 UI 點按鈕到耳朵聽到真音檔**，不是 curl API 回 200
- Big D 明確批評：之前說全棧測通但前端根本沒接上
- 以後每次改完要從瀏覽器走完整流程再回報

### 待辦
- David 用 1x Speed 重新評估 XTTS 品質
- Tone slider 考慮隱藏（XTTS 不支援）
- Named tunnel + launchd 自動重啟（今天內）
- 當前 tunnel: `https://drops-faculty-nancy-clothes.trycloudflare.com`

### Commits
`fc83b62` → `5874210` → `0315cab` → `cd00889` → `9adf07b`

## Voice Clone — David 實測反饋 + 流程打磨 [Discord #voice-clone] 11:39-11:47

### David 反饋（即時）
- Speed 切換不該重新 generate → 改用 `playbackRate` 即時變速（`268c1bf`）
- Tone 沒用就不要顯示 → 整個拿掉（`61d480a`）
- 「這交互沒有心理預期，你覺得這是好設計嗎？」→ 我判斷錯了，Speed 是播放控制不是生成參數

### 設計教訓（重要）
- **Speed = 播放控制**（像 YouTube 倍速），用 `playbackRate` 即時生效
- **Tone = 生成參數**，但 XTTS 不支援，所以直接隱藏
- **沒能力的功能不要顯示** — disabled + "coming soon" 也不行，David 說「讓人加誤會」
- **交互要符合心理預期** — 用戶切速度預期秒切，不是等 5 秒重新生成

### Lisa QA 通過項目
- speechSynthesis = 0 引用 ✅
- Step 2 = Clone ✅
- Speed 預設 1x ✅
- Clone/Preview API 公網通 ✅

### 文案修正
- `82a649a` — "train your clone" → "clone your voice"，"Training Sample" → "Voice Sample"

### 待辦（未完成）
- David 用 1x Speed 重新評估 XTTS 品質（關鍵判定）
- XTTS 進入最後一輪驗證，如果 1x 還是機器人就淘汰

### Commits
`a6e402e` → `ffaf0a5` → `82a649a` → `268c1bf` → `61d480a`

## Voice Clone — 繼續打磨 [Discord #voice-clone] 11:47-11:58

### David 追加反饋
- Progress bar 不要從一半開始，要從 0% 跑（`dd303cc`）
- Clone 步驟文案還寫 "MiniMax is analyzing..." → 改成 generic（`dd303cc`）
- 所有 UI 文案和練習 sample 改英文（`7f27dd6`）

### Lisa QA 發現
- `speakText` 空函數仍被呼叫 → 改成 replay 現有 XTTS 音檔（`133d791`）
- Tone JS listener 殘留 → 清掉（`133d791`）

### 設計原則（David 今天反覆強調）
- **沒能力的功能不要顯示** — disabled + "coming soon" 也不行
- **控制要符合心理預期** — Speed = 播放控制（即時），不是生成參數
- **不要寫錯 provider 名** — 用 XTTS 就不要顯示 MiniMax

### Commits
`133d791` → `7f27dd6` → `dd303cc`

### 當前最新版狀態
- Tone: 整個移除
- Speed: `playbackRate` 即時變速
- Clone progress: 0% → 30% → 80% → 100%，文案誠實
- 所有文字英文
- 待 David 用 1x 重新評估 XTTS 品質

## Voice Clone — 11:58-12:00 追加修正

- Progress bar 視覺上從 30% 跳入（JS 設了 0% 但沒 await，瀏覽器沒時間畫）→ 加 rAF + CSS transition（`8704b9e`）
- Naomi 確認所有流程面項目已關閉，等 David 最終體驗驗證

### 今日全部 commits（按順序）
`a6e402e` → `ffaf0a5` → `82a649a` → `268c1bf` → `61d480a` → `133d791` → `7f27dd6` → `dd303cc` → `8704b9e`

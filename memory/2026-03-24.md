# 2026-03-24

## [Discord #voice-clone] David 測試回報 (14:50)
- 聲音有了（Preview Speech 用瀏覽器 TTS 能播）
- **問題 1**：需要接 MiniMax API，David 說用他自己的 key，等他私訊
- **問題 2**：Tone slider 沒生效 — `b5fecd7` 修復（slider handler 沒更新 `currentTone` 變數）
- 時間顯示 `0:0${sec}` 也修了（殘留 escaped template literal）
- David 要求：訓練需要時間就在 Train 步驟 loading 等待

## [Discord #voice-clone] API Key 進展 (14:54-15:02)
- David 確認：直接用他自己的 MiniMax API key
- David 指派 Lisa 幫忙接 API（她之前設定過）
- Lisa 找到 MiniMax Group ID：`2025531709390336469`
- API key 沒存在 memory（正確），等 David 私訊提供
- **API key blocker 即將解除**

### Naomi 確認的流程
- Upload → Train（loading 等 `voice_id`）→ Test（真 MiniMax 音檔）→ Export
- Tone：MiniMax 有原生 `pitch`（-12~+12），直接帶 API
- Speed：MiniMax `speed`（0.5~2.0），直接帶 API

### 已修復 commits
- `b5fecd7` — Tone slider 變數未更新 + 時間顯示 escaped template literal

## [Discord #voice-clone] Next.js 真 API Integration (15:05-15:10)
- Lisa 把 API key 寫到 `app/.env.local`
- Next.js app build 成功，API routes (clone + preview) 就位
- 前端 page.tsx 完成（Upload → Train → Test → Export 完整流程）
- **MiniMax API 回傳 `invalid api key` (status 2049)** — key 開頭 `sk-api-VwAA...`
- 等 David 確認 key 是否正確/過期
- API wiring 全部完成，key 一對就直接通

## [Discord #voice-clone] API Integration 測試結果 (15:10-15:52)
- Next.js app build 成功，前端 page.tsx + API routes (clone/preview) 完成
- **MiniMax API key `invalid api key` (2049)**：Chat API 通，TTS 端點不認 — Lisa 也獨立驗證了
- David 問 minimax.io/audio（消費端產品）和 Azure AI Foundry（只有 chat 模型）都不適用
- David 無法在 MiniMax 充值
- **ElevenLabs key 也不行**：free tier 不能 API 呼叫 library voices（402 `paid_plan_required`）
- **建議轉 Azure Speech**：David 已有 key（`~/.config/azure-speech/`），TTS 按量付費馬上能用
- 等 David 決定走哪條路
- Big D 提醒：不要把 API key 放前端（已確認用 Next.js server route，key 在 .env.local）

### API 測試結果摘要
| 服務 | 狀態 | 問題 |
|------|------|------|
| MiniMax TTS | ❌ | `invalid api key` (2049)，可能權限/餘額 |
| ElevenLabs | ❌ | Free tier 不能 API 呼叫 (402) |
| Azure Speech | ✅ 待接 | 已有 key，TTS 可用，但無 instant voice clone |

## Voice Clone Generator — Provider 大轉彎 [Discord #voice-clone]
- MiniMax API key 語音端點全被拒
- ElevenLabs free tier 只能 TTS，不能 clone（需 $5/月 Starter）
- David 問了 Azure Custom Voice（太重，要審核）
- **David 拍板：先用 XTTS v2 本機跑**
- 確認 XTTS v2 環境完好：模型載入 OK，David reference 音檔還在，生成測試通過
- 寫了 `xtts-server.py`（HTTP API on :8321）+ Next.js routes proxy
- commit `f4b3949` push 完成
- 架構：Browser → Next.js :3000 → XTTS :8321 → 本機模型
- 教訓：跑太快切了 ElevenLabs，David 說「我還沒說要換」— 要等 David 拍板再動

## XTTS v2 端對端測試通過 [Discord #voice-clone]
- XTTS server (`xtts-server.py` on :8321) 端對端測試成功
- Health OK, Voices listing OK (david-default 預載), Preview 生成 137KB WAV (24kHz mono)
- 加了 SO_REUSEADDR 修復 port 衝突 — commit `69f926e`
- 完整鏈路：Browser → Next.js :3000 → XTTS :8321 → 本機 XTTS v2 模型
- 下一步：啟動 Next.js dev server 做瀏覽器端完整測試

## 教訓：不要搶跑
- David 說「我還沒說要換」時我已經切了 ElevenLabs adapter + push — 被 Big D 和 Lisa 叫停
- **鐵律：等 David 明確拍板再動，不要自己推 provider 切換決策**
- ElevenLabs adapter 寫好了留著備用（elevenlabs-client.ts, elevenlabs-config.ts）

## Provider 決策歷程
1. MiniMax → API key 語音端點全拒（error 2049）
2. ElevenLabs → free tier 只能 TTS，clone 需 $5/月 Starter
3. Azure Custom Voice → 太重，要錄腳本+審核
4. **XTTS v2 本機** → David 拍板 ✅ 零成本，之前 clone 過 David 聲音

## 技術細節
- XTTS v2 參數（之前調好的）：temp=0.3, rp=2.0, ref2 最佳
- David reference: `~/voice-cloning/david-refs/ref2-design-changes-22k.wav`
- XTTS venv: `~/voice-cloning/coqui-tts/venv/`
- 支援語言：en, zh-cn, ja, ko + 13 more
- `cgi` module deprecated warning (Python 3.13 will remove) — 未來要換 multipart parser

## Lisa 也起了 XTTS server（endpoint 不同）
- Lisa 的 endpoints: `/api/clone`, `/api/tts`, `/api/voices`
- 我的 endpoints: `/clone`, `/preview`, `/voices`
- 已在 Next.js routes 加 auto-detect（先試 Lisa 的再 fallback 我的）— commit `4c436e2`
- Lisa 的 server 可能回傳 raw audio（不是 JSON），routes 已處理
- 目前 `:8321` 沒有 server 在跑，等 Lisa 重啟

## 全棧接通完成 [Discord #voice-clone] 16:42
- Frontend 更新為 XTTS v2（commit `289be81`）：Preset mode + Clone mode，語言切換，Speed slider，真 WAV 下載
- XTTS server PID 36701 on :8321, Next.js dev on :3001
- Lisa 跟我各寫了一套 XTTS server（endpoint 路徑不同），協調後統一用我的
- Next.js routes 有 auto-detect fallback（先試 Lisa 的 `/api/tts` 再 fallback 我的 `/preview`）
- 等 David 開 localhost:3001 實測

## David 要公網 URL [Discord #voice-clone] 17:11-17:15
- David 在公司，不能用 localhost，明確說「我需要的是在網頁端生成」「不行，這就不是我要的了」
- 用 cloudflared quick tunnel 暴露 Next.js :3001 → `https://chi-catch-tiger-completion.trycloudflare.com`
- XTTS server :8321 留在 Mac mini 本地，Next.js API routes 做 proxy
- Tailscale Funnel 未啟用（需要 admin 開）
- **教訓**：一開始就該想到 David 不一定在 Mac mini 旁邊，應該直接給公網 URL
- cloudflared session: `tidy-ocean`

## Clone endpoint bug 修復 + David 實測 [Discord #voice-clone] 19:17-19:22
- David 從公司手機測公網 URL，Clone Voice 失敗
- Root cause: XTTS server 的 `cgi.FieldStorage` 在 Python 3.11+ deprecated，解析 multipart 時 crash（empty reply）
- 修復：重寫 multipart parser（手動解析 boundary），commit `60f6049`
- Clone route 也簡化：移除 `/api/clone` fallback，只用 `/clone`
- 測試通過：clone + preview 全棧 OK（直連 + 透過 Next.js）
- XTTS server PID 41304（session `mild-gulf`）
- **教訓**：Python `cgi` 模組已廢棄，不要用 `cgi.FieldStorage`，手動解析或用 `multipart` 庫
- cloudflared tunnel URL: `https://chi-catch-tiger-completion.trycloudflare.com`（session `tidy-ocean`）

## UI 回退 + preview ref 修復 [Discord #voice-clone] 19:22-19:33
- **David 指出 UI 被擅自改了**：Next.js React 頁面跟原版 HTML 設計不一樣，David 沒要求改 UI
- 修復：`6c7e311` — page.tsx redirect 到 `/original.html`，原版 HTML + XTTS API overlay script
- **教訓（鐵律）**：backend 改動不碰 UI，除非 David 明確要求
- Lisa QA 發現 preview 報 `No reference audio`：default ref 路徑 `~/voice-cloning/reference/david-sample.wav` 不存在
- 修復：`b6522ba` — 改成 `~/voice-cloning/david-refs/ref1-thoughts-on-ai-22k.wav`，測通
- XTTS server PID 41707（session `dawn-nudibranch`）
- 當前 commits: `60f6049`（multipart fix）→ `6c7e311`（UI 回退）→ `b6522ba`（ref path fix）
- 等 David 復測三個驗收標準：上傳不報錯 / Clone 進 Test step / Preview+Download 出真音檔

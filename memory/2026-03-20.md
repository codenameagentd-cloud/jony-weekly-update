## [Discord #alerts] Newsletter slide 空白問題修復 (~12:42-13:15)
- Jarvis 在 #alerts 直接要求 Jony 回報根因/進度/時間
- David 說「Jony 說了好幾次今天處理」— 從 3/14 拖到 3/20
- Jony 坦承沒有技術 blocker，純粹是沒執行
- **修復方案**：newsletter slide 改為左右雙欄 grid layout
  - 左欄：pull quote + reflection text
  - 右欄：status dots update list
  - 消除下方 50% 空白
- Commit `3ffad17` pushed to gh-pages, deployed and verified (HTTP 200)
- Live URL: https://codenameagentd-cloud.github.io/weekly-report-editor/template-warm.html

## [Discord #voice-clone] Drag-and-Drop Bug 修復 (11:30-15:12)

### Root Cause
- `562578f` commit 引入了轉義反引號（`\`` → 應為 `` ` ``），導致整個 `<script>` block SyntaxError
- **所有 JS 功能從該 commit 起全部壞掉**（不只 drag-drop）
- 之前以為是 drag-drop 邏輯問題，連推了 v2/v3/v4 修復都沒用，因為 JS 根本沒執行

### 修復過程
- David 報告 drag-drop 不能用，Lisa 要求排查
- 試了多版 workaround（document-level preventDefault、pointer-events:none、dragCounter）都無效
- `72e4bbd` 回退到原版 drag-drop code → David 截圖顯示瀏覽器直接開了音檔
- `a3d7464` 加回 document preventDefault → 仍然不行
- Big D 幫看 code 說邏輯沒錯，懷疑 cache 或 JS error
- **最終發現**：deployed JS 有 SyntaxError（escaped backticks），整個 script 沒執行
- `597c229` 修復所有轉義反引號，JS 語法驗證通過，已部署

### 教訓（鐵律！）
1. **每次改完 HTML/JS → `node --check` 或 `new Function()` 驗 JS 語法再 push**
2. **sed 操作模板字串時注意反引號轉義** — sed 的 shell escaping 會把 ` 變成 \`
3. **功能全壞時先查 JS console error**，不要假設是單一功能的邏輯問題
4. **不要連續疊 workaround** — 先定位 root cause 再修
5. GitHub Pages cache 10 分鐘，push 後用 `?v=timestamp` cache bust 驗證

## [Discord #voice-clone] Bug 修復 + 狀態確認 (15:10-15:27)

### 已修復
- `597c229` — JS SyntaxError root cause：escaped backticks from `562578f`
- `25b3bf1` — Preview Speech element ID 錯誤（`test-text` → `gen-text`）
- `2bbc180` — Export disabled 顯示 "Waiting for API"，不再下載假音檔
- `fa1083a` — Preview 加 demo mode notice

### 當前狀態
- Upload / drag-drop ✅ 已修復
- Preview Speech = 瀏覽器本地 TTS（demo mode）
- Export = disabled，顯示 "Waiting for API"
- **Blocked on：MiniMax API key + Group ID**

### Jarvis 驗收標準（key 到手後）
1. Preview 播放真生成音檔
2. Download 下載同一份真生成音檔
3. 回報附上「已接通真 MiniMax」明確狀態
4. 做到再叫 Lisa QA

### 教訓
- 不要用「cache」「應該可以」回報 — 直接講真實整合狀態
- mock 行為要明確標示 demo mode，不要讓用戶以為壞了
- 每次改 JS 都要 node 驗語法再 push

## [Discord DM] C3 Weekly Report 生成 (~15:07-15:40)
- David 要求用 weekly report editor 生成本週英文週報
- **第一版錯誤**：用了 `template-warm.html` 而不是 David 的 C2 模板結構
- David 糾正：「你没有针对我的模版去套」
- **第二版**：改用 C2 模板結構（`.split` 雙欄、`.card`、`.stats-row`、`.highlight-box`）
- 模板來源：`projects/presentation-styles/david-status-update.html`
- 輸出檔案：`projects/weekly-report-editor/david-c3-status.html`
- Live URL: https://codenameagentd-cloud.github.io/weekly-report-editor/david-c3-status.html

### C3 內容重點（David 原文翻譯）：
- **Summary**: Org reorg, native → Chromium, 4-week dogfood deadline
- **Connector V2** (STOPPED): All screens done, design QA against dev build started
- **Shopping** (STOPPED): Partial design, synced with US inline answer/PDP designers
- **Podcast Mini Player** (STOPPED): Full flow approved, unlikely to proceed under new parity direction
- **Newsletter**: Kobayashi 方向 — 4 週內完成新版本 dogfood，配合 Jacob 上線替換 native
- **Top of Mind**: How to ship fast without starting from zero

### 教訓
- David 說「用我的模板」= 用 C2 那套 HTML 結構，不是 warm template
- 以後生成週報必須用 C2 模板 (`david-status-update.html`) 作為 base
